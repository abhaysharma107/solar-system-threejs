# Abhay's System — Portfolio Project

- This is an interactive 3D portfolio project built with Vite as the bundler.
- Source code lives in `src/` with `src/main.js` as the entry point.
- Use `npm run dev` to start the development server.
- Use `npm run build` to create a production build.
