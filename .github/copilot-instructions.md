# Three.js Project

- This is a Three.js project built with Vite as the bundler.
- Source code lives in the root directory with `main.js` as the entry point.
- Use `npm run dev` to start the development server.
- Use `npm run build` to create a production build.
